# Application Mode
# Options: 'dev' or 'live'
# In 'dev' mode, API requests are proxied to http://localhost:3000/api
# In 'live' mode, API requests go to VITE_API_URL_LIVE
VITE_APP_MODE=dev

# Production API URL (used only when VITE_APP_MODE=live)
VITE_API_URL_LIVE=http://144.124.249.43:4000/api
